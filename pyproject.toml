[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["dataset/raw", ".venv*", "out", "checkpoints"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra -q"
